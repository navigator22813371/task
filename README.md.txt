Данная задача была написана около полтара а то и два года назад. 3 человека в ней участвовало пробовали работу в командеб  Много что забыл так как не программировал все это время теперь надо с начала все вспоминать.

Проект делали в две части отдельно БЭК и отдельно Фронт за основу брался принцип CODE FIRST это когда пишешь код модели а затем делаешь миграцию в БД (add-migration)  и после выполнения этой команды в папку Migrations будет добавлена новая миграция и просто как любой проект дальше запускается F5 или ctrl+f5 кто как (все модели находятся в папке [Core] ) в папке [Core] есть абстрактный класс выведен отдельно и насладуется почти все от этого класса так как для всех классов все одинаково
в Application использовались DTO в основном все в проекте делалось через DTO есть автомапер 


в Application есть Logics для каждого подразделения внутри была создана папка если любую открыть к примеру Characters то делится все еще на 2 папки Command где есть работа с базой именно внутни нее добавление удалиние обнавление и есть Queries тоесть получить данные 


API
Controllers  


Swagger ну тут не чего говорить просто чтобы протестировать было работает код или нет для больших целей его не использовал


за докер не чего сказать не смогу так как я с ним не работал 
с токенами и авторизацией на  проекте тоже не работал
а в остальном то что вспомнил 

Postgresql использовался в проекте, в основном всегда использовал MSSQL ссылку на еще один проект декстопный прикреплю c БД который писал еще раньше чем этот  

 

